  | <%= link_to raw('<i class="list circle icon"></i>Unit List'), admin_course_units_path(resource) %>

  | <%= link_to raw('<i class="add circle icon"></i>Add Unit'), new_admin_course_unit_path(resource) %>
  | <%= link_to raw('<i class="add icon"></i>Clone Course'), clone_admin_course_path(resource) %>


<% if resource.published %>


  | <%= link_to raw('<i class="forward mail icon"></i>Undo publish'), publish_admin_course_path(resource), :remote => true %>  

  <% else %>
  | <%= link_to raw('<i class="forward mail icon"></i>Publish'), publish_admin_course_path(resource), :remote => true %>

<% end %>
